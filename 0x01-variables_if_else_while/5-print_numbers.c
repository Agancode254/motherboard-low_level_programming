#include <stdio.h>

int main(void)
{
    int r = 0; // Set r to 0

    // Loop to print single-digit numbers from 0 to 9
    for (int number = 0; number < 10; number++)
    {
        printf("%d\n", number); // Print the current number followed by a new line
    }

    return 0; // Indicate successful execution
}

