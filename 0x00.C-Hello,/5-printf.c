#include <stdio.h>

int main(void)
{
    /* Use printf function to print the string with proper grammar */
    printf("%s, but the outcome is a piece of art,\n", "with proper grammar");

    /* Return 0 to indicate successful execution */
    return (0);
}

